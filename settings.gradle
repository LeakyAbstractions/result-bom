
rootProject.name = 'result-bom-root'
include('result-bom')
include('catalog')

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("./gradle/result.versions.toml"))
        }
    }
}
